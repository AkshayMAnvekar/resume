%====================
% PERSONAL PROJECT
%====================

\subsection{Self-Hosted Edge AI Agent on NVIDIA Jetson \texorpdfstring{\hfill}{ } 2025}
\begin{zitemize}
\item Built a private, self-hosted AI agent on NVIDIA Jetson using \textbf{llama.cpp and CUDA} to run quantized language models locally with tool calling support.
\item Containerized the model server, vector database, REST API, and user interface with Docker, enabling fully offline operation without cloud LLM dependencies.
\item Optimized model quantization levels and context settings to balance memory usage, response speed, and accuracy on edge hardware.
\end{zitemize}
