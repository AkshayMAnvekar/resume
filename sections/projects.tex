%====================
% PERSONAL PROJECT
%====================

\subsection{Self-Hosted Edge AI Agent on NVIDIA Jetson \texorpdfstring{\hfill}{ } 2025}
\begin{zitemize}
\item Built a private, self-hosted AI agent on NVIDIA Jetson, using \textbf{llama.cpp and CUDA} to run quantized language models locally and support tool calling.
\item Packaged the model server, vector database, REST API, and user interface with Docker, enabling offline use without relying on hosted LLM services.
\item Tuned quantization levels and context settings to balance model quality, memory use, and response time on resource-constrained hardware.
\end{zitemize}
